# Foundry build output and dependencies (reproduced by `forge soldeer install` + `forge build`)
out/
cache/
dependencies/
broadcast/
# Local deployment build info and smoke evidence (generated; never committed)
runtime/
